'use strict';

const test = require('ava');
const utils = require('../lib/utils');

test('extrallendo hashtags de text', t => {
  let tags = utils.extractTags('a #imagenes con tags #AwEsOmE #Sena #AVA and # #100 ##yes');

  t.deepEqual(tags, ['imagenes', 'awesome', 'Sena', 'ava', '100', 'yes']);

  tags = utils.extractTags('imagenes sin tags');
  t.deepEqual(tags, []);

  tags = utils.extractTags();
  t.deepEqual(tags, []);

  tags = utils.extractTags(null);
  t.deepEqual(tags, []);
});
//# sourceMappingURL=..\node_modules\.cache\ava\bb72e67b86bed82520cec40cd5dcf5f0.js.map